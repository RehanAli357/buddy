import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#f4f2ff",
    },
    secondary: {
      main: "#4B0082",
    },
    text: {
      primary: "#ff4500",
      secondary: "#ffd700",
    },
  },
  typography: {
    fontFamily: "'Luckiest Guy', cursive",
  },
});
